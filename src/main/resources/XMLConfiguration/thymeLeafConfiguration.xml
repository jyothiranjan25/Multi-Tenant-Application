<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <mvc:annotation-driven/>

    <!-- Thymeleaf Template Engine -->
    <bean id="thymeleafTemplateEngine" class="org.thymeleaf.spring6.SpringTemplateEngine">
        <property name="templateResolver" ref="thymeleafTemplateResolver"/>
    </bean>

    <!-- Thymeleaf View Resolver -->
    <bean class="org.thymeleaf.spring6.view.ThymeleafViewResolver">
        <property name="templateEngine" ref="thymeleafTemplateEngine"/>
        <property name="characterEncoding" value="UTF-8"/>
    </bean>

    <!-- Thymeleaf Template Resolver -->
    <bean id="thymeleafTemplateResolver" class="org.thymeleaf.spring6.templateresolver.SpringResourceTemplateResolver">
        <property name="prefix" value="/myApp/static/" />
        <property name="suffix" value=".html" />
        <property name="templateMode" value="HTML" />
        <property name="characterEncoding" value="UTF-8" />
    </bean>

    <!-- React App static Files Configuration -->
    <mvc:resources mapping="/static/**" location="/myApp/static/" />
    <mvc:resources mapping="/public/**" location="/myApp/public/"/>

    <!-- MVC view resolver -->
    <mvc:view-controller path="/" view-name="login"/>
    <mvc:view-controller path="/login" view-name="login"/>
    <mvc:view-controller path="/dashboard" view-name="Dashboard"/>
    <mvc:view-controller path="/switchRoles" view-name="SwitchRoles"/>
    <mvc:view-controller path="/selectResources" view-name="SwitchResources"/>

    <!-- User Management -->
    <mvc:view-controller path="/userGroup" view-name="UserGroup"/>
    <mvc:view-controller path="/resources" view-name="Resources"/>
    <mvc:view-controller path="/modules" view-name="Modules"/>
    <mvc:view-controller path="/roles" view-name="Roles"/>
    <!-- End User Management -->
</beans>